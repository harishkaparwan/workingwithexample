const mapDispatchToProps = (dispatch, ownProps) => {
  console.log('ownProps in mapDispatchToProps:', ownProps);

  return {
    changePredefinedQuickFilters: (filters) => dispatch(filterActions.genericChangePredefinedQuickFilters(filters)),
    searchJourneys: () => dispatch(journeyActions.handleSearchJourneysWizard(ownProps.namespace, ownProps.settingsNamespace, ownProps.moduleSettingsNamespace))
  };
};
